# 客户端数据划分方案

> 版本：v2.1
> 更新：2026-08-11（改为按起始时间划分）
> 适用范围：个性化联邦学习实验的客户端定义与数据划分
> 目标读者：项目全体成员

---

## 1. 总体原则

- 单一共享 TCN encoder + 本地个性化 MLP head
- 类别信息通过**类别特征编码**注入 encoder（0=居民，1=变压器，2=钢铁工业）
- encoder 聚合采用标准 **FedAvg 按数据量加权**
- 数据集划分严格按时间顺序（不 shuffle）
- 划分比例：**80% 训练 / 20% 测试**（不设验证集，直接训练/测试划分）

---

## 2. 客户端划分

**快速实验方案：9 个客户端**（减少通信轮次，加快迭代速度）。完整 72 客户端方案见 v1.2 历史版本。

| 数据源       | 客户端数 | 划分规则                     | 类别特征    | 本地特征维度 |
| ------------ | -------- | ---------------------------- | ----------- | ------------ |
| eld_ind      | 3        | 按投运起始年份分档           | 1（变压器） | 0            |
| lcl_res      | 2        | 按首次记录时间（2012年为界） | 0（居民）   | 0            |
| steel_ind    | 1        | 整个数据集                   | 2（工业）   | 4            |
| tetouan_city | 3        | 1 个分区/客户端              | 0 / 2       | 5            |

**合计：9 个客户端。**

### 2.1 eld_ind — 3 客户端（按投运起始年份）

- 清洗后实得 **361 列** MT，全部通过连续性检查（内部无 >24h 缺口），全部保留
- NaN 为 leading NaN（变压器晚于数据集起始 2011-01-01 投运），非数据质量问题
- 按首次有效记录时间（即变压器投运时间）分为 3 档：

| 客户端      | 投运年份    | 序列数 | 说明                |
|------------|------------|-------|---------------------|
| eld_ind_0  | < 2012     | 156   | 早期投运（2011年前）|
| eld_ind_1  | 2012       | 170   | 2012年投运          |
| eld_ind_2  | >= 2013    | 35    | 近期投运（2013~2014）|

- 全序列时间跨度：2011-01-01 ~ 2015-01-01 @30min，70,129 步
- 投运时间跨度：2011-01-01 ~ 2014-07-18，57% 在 2011 年之前，47% 在 2012 年
- 尾部 NaN 极少（仅 2 列），来自变压器提前退役

### 2.2 lcl_res — 2 客户端（按首次记录时间）

清洗后 **321 户全部有效**（NaN≤50%）。

NaN 为 leading NaN（用户晚于数据集起始 2011-11-23 安装智能电表），非数据质量问题。按首次有效记录时间分为 2 波：

| 客户端     | 加入时间        | 序列数 | 说明              |
|-----------|----------------|-------|-------------------|
| lcl_res_0 | < 2012-01-01   | 174   | 早期加入（2011试点首批）|
| lcl_res_1 | >= 2012-01-01  | 147   | 后续加入（2012扩展批）|

- 加入时间跨度：2011-11-23 ~ 2012-10-22，2011-11~12 为第一波（174户），2012-01~03 为第二波（~133户），其余散落
- 2011-12 与 2012-01 之间存在天然断档（1月仅 1 户），是最干净的分割线
- 原生采样间隔 30min，**无需重采样**
- **本地特征维度 0**：`tariff_type` 在特征筛选中被剔除（该公开子集全为常量 Std，无 ToU 用户）

### 2.3 steel_ind — 1 客户端

- 整个数据集算 1 个客户端，1 条序列
- 采样间隔 15min，统一重采样到 **30min**（每 2 行取均值）
- 序列长度：17,520 步（365 天 @30min，2018-01-01 ~ 2018-12-31）
- 类别特征：2（钢铁工业）
- **本地特征维度 4**：`lagging_reactive_power`、`co2`、`lagging_pf`、`load_type`
  - 剔除：`leading_reactive_power`（SHAP 0.24，<1%）、`leading_pf`（SHAP 0.21，<1%）

### 2.4 tetouan_city — 3 客户端

- Zone 1（工业区）、Zone 2（居民商业混合）、Zone 3（居民商业）各算 1 个客户端
- 每个客户端 1 条序列
- 采样间隔 10min，统一重采样到 **30min**（每 3 行取均值）
- 序列长度：17,472 步（364 天 @30min，2017-01-01 ~ 2017-12-30）
- 类别特征：Zone 1 → 2（工业），Zone 2、3 → 0（居民）。**注意**：清洗后 csv 的 `category_id` 列为常量 1（错误），建模脚本必须按客户端覆盖
- **本地特征维度 5**：`temperature`、`humidity`、`wind_speed`、`general_diffuse_flow`、`diffuse_flow`

---

## 3. 数据划分（每序列独立执行）

所有序列按 **8:2** 比例、严格时间顺序切分：

```
|<--- train 80% --->|<-- test 20% -->|
```

- 不设验证集，固定 epoch 训练
- 划分以序列为单位执行（不是以客户端为单位）
- 同客户端内的多条序列各自独立切分
- 不 shuffle，保证时间因果性

---

## 4. FedAvg 聚合权重

按每个客户端的数据量（总有效步数）加权：

| 组                       | 步数占比 | 说明                           |
| ------------------------ | -------- | ------------------------------ |
| 3 个 eld_ind 客户端      | ~72%     | 合计 361 序列，集中在 eld_ind_0（156 MT）和 eld_ind_1（170 MT）|
| 2 个 lcl_res 客户端      | ~28%     | 合计 321 序列，两个客户端较均衡（174/147）|
| steel_ind + tetouan_city | <0.2%    | 每客户端 1 条序列              |

eld_ind_0 和 eld_ind_1 在聚合中占主导。steel_ind 和 tetouan_city 权重极低，核心价值在**评估时验证跨域泛化能力**。

---

## 5. 特征分工

### 5.1 公共特征（Global TCN 输入，8 维）

所有 9 个客户端统一派生，维度与编码见《数据清洗.md》§5.1：

| 特征 | 编码 | 含义 |
|------|------|------|
| hour_sin | sin(2π·hour/24) | 小时正弦 |
| hour_cos | cos(2π·hour/24) | 小时余弦 |
| dow_sin | sin(2π·dow/7) | 星期正弦 |
| dow_cos | cos(2π·dow/7) | 星期余弦 |
| is_weekend | 0/1 | 周末标志 |
| month_sin | sin(2π·(month-1)/12) | 月正弦 |
| month_cos | cos(2π·(month-1)/12) | 月余弦 |
| category_id | 0/1/2 | 类别编码 |

### 5.2 本地特征（Residual Corrector 输入）

特征筛选后各数据集的本地特征（完整筛选依据见 `data/feature_selection/`）：

| 数据源 | 本地特征维度 | 具体列 | 说明 |
|--------|------------|--------|------|
| eld_ind | **0** | — | 无原生非功率列 |
| lcl_res | **0** | — | `tariff_type` 为常量，已剔除 |
| steel_ind | **4** | `lagging_reactive_power`, `co2`, `lagging_pf`, `load_type` | 剔除2列：`leading_reactive_power`(SHAP<1%)、`leading_pf`(SHAP<1%) |
| tetouan_city | **5** | `temperature`, `humidity`, `wind_speed`, `general_diffuse_flow`, `diffuse_flow` | 全保留（VIF<5，物理意义明确） |

各客户端 Corrector 输入维度（输入沿特征维度拼接：`Y_pre` + 近期真实历史 + D_local）：

| 数据源 | Corrector 特征维度 (2 + D_local) |
|--------|--------------------------------|
| eld_ind | 2 + 0 = **2** |
| lcl_res | 2 + 0 = **2** |
| steel_ind | 2 + 4 = **6** |
| tetouan_city | 2 + 5 = **7** |

---

## 6. 变更记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | — | 初版：62 客户端（eld 37 + lcl 21 + steel 1 + tetouan 3） |
| v1.1 | — | 补充特征分工、聚合权重、本地特征维度 |
| v1.2 | 2026-08-10 | 基于清洗后实际数据修订：eld 361列全有效，客户端 37→36；lcl 321户，客户端 21→32；总客户端 62→72。steel保留列更新。澄清首尾NaN非质量问题。 |
| v2.0 | 2026-08-10 | 快速实验方案：eld 36→3 客户端（按有效天数分档），lcl 32→2 客户端（730天分界），总客户端 72→9。Corrector 输入维度修正为 2+D_local（去除 4 lag）。 |
| v2.1 | 2026-08-11 | 改为按起始时间划分：eld 按投运年份（<2012 / 2012 / >=2013），lcl 按加入批次（<2012-01-01 / >=2012-01-01）。eld 无需丢弃序列（全 361 列保留）。 |

---

*本方案 v2.1，按起始时间划分。完整 72 客户端方案见 v1.2。随数据迭代修订。*
