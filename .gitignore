#大型数据文件
data/processed
data/raw

#编译文件
__pycache__/
*.py[cod]
*.pyo
*.so
.pytest_cache/

#Zcode工作文件
AGENTS.md

#claude工作文件
.claude
CLAUDE.md
fl_code/outputs/
superpowers/
.superpowers

#测试文件
test/

#pt模型
*.pt

#联邦学习输出产物（Phase 2/3 运行结果，不入库）
fl_code/baseline_outputs/
fl_code/personalized_outputs/
app/runs/
.env

#web 服务端 RC 对比图上传（运行时生成，不入库）
server/rc_uploads/

#App 运行产物（不入库）
data/fl_server.db
app/data/

#单机多客户端：每个客户端独立的 数据/阶段状态/工作 目录（运行时生成，不入库）
app/data_*/
app/stage_*/
app/rc_work_*/

#单客户端默认目录（未设 FL_STAGE_DIR / FL_RC_WORK 时生成，不入库）
app/stage/
app/rc_work/

#二阶段/三阶段产物（修正器权重与预测 JSON，不入库）
app/rc_work*/

#flwr worker 运行产物（MODEL_DIR 指向仓库 data 目录时生成，不入库）
data/fl_model_*.pkl
data/fl_worker_*.log
